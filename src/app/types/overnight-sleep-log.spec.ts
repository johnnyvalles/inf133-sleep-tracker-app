import { OvernightSleepLog } from './overnight-sleep-log';

describe('OvernightSleepLog', () => {
  it('should create an instance', () => {
    expect(new OvernightSleepLog()).toBeTruthy();
  });
});
